import './header.css'

export const Header = ({text}) => {
  return (
    <header id="header">
      <h1 id="h1">{text}</h1>
      {/* <button id="buttonforsidebar">tes</button> */}
    </header>
  );
};
